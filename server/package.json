{
  "name": "finova",
  "version": "1.0.0",
  "description": "Finova - Full-Stack Digital Banking & Investment Platform",
  "private": true,
  "scripts": {
   
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm install && npm run build",
    "setup-db": "cd server && node -e \"require('dotenv').config(); const pool = require('./db/pool'); const fs = require('fs'); const sql = fs.readFileSync('./db/init.sql','utf8'); pool.query(sql).then(()=>{console.log('Tables created');process.exit(0)}).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
